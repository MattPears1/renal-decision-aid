# Server Configuration
PORT=5006
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins, or leave empty for defaults
CORS_ORIGIN=http://localhost:3006,http://localhost:5173

# OpenAI Configuration
# Required for AI chat functionality
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Session Storage Configuration
# Options: memory (default), file, redis
# - memory: In-memory storage (lost on restart, good for development)
# - file: File-based storage (persists across restarts within dyno lifecycle)
# - redis: Redis storage (recommended for production with Heroku Redis addon)
SESSION_STORAGE=memory

# File-based session storage path (only used when SESSION_STORAGE=file)
SESSION_FILE_PATH=./sessions.json

# Redis Configuration (only used when SESSION_STORAGE=redis)
# Heroku sets REDIS_URL automatically when you add the Redis addon
# REDIS_URL=redis://localhost:6379

# Logging Configuration
# Log levels: error, warn, info, debug
# Production defaults to 'info', development defaults to 'debug'
LOG_LEVEL=debug
